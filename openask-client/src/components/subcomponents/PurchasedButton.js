import React from "react";
import { Box, Typography } from "@mui/material";

const PurchasedButton = () => {
  return (
    <Box className="purchase-btn">
      <Typography>Purchased</Typography>
    </Box>
  );
};

export default PurchasedButton;
