import React from "react";
import { Box, Typography } from "@mui/material";

const AnswerButton = () => {
  return (
    <Box className="question-answer-btn">
      <Typography>Answer</Typography>
    </Box>
  );
};

export default AnswerButton;
